'use strict';

/**
 * Created by veeramuthum on 7/14/16.
 */

function isPrime(n) {
    return true;
}

for(var i=0; i<=9; i++) {
    if(isPrime(i)) {
        console.log(i + " is prime");
    } else {
        console.log(i + " is not prime");
    }
}
